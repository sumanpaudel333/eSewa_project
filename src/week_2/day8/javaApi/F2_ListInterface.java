package week_2.day8.javaApi;

public class F2_ListInterface {
    /*
    - It is the child interface of Collection
    - If we want to represent a group of individual objects as a single entity where duplicates are allows and
    insertion order must be preserved then we should go for List.
    - We can differentiate duplicates by index.
    - We can preserve insertion order by using index, hence index play very important role in list interface.
     */

    /*
    void add(int index, Object o)g
    boolean addAll(int index, Collection c)
    Object get(int index)
    Object remove(int index)
    Object set(int index, Object new) // to replace the element present at specified index with provided Object and
    returns old object
    int indexOf(Object o) // returns index of first occurrence of 'o'
    int lastIndexOf(Object o)
    ListIterator listIterator()
     */
    public static void main(String[] args) {

    }
}
